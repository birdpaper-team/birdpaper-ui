import { EventTableItem } from "../../../components/table-block/src/tableBlock";

const list: EventTableItem[] = [
  { name: "load", remark: "图片加载完成触发", params: "none" },
  { name: "error", remark: "图片加载失败触发", params: "none" },
];

export default list;

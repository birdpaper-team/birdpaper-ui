import { PropTableItem } from "../../../components/table-block/src/tableBlock";

const list: PropTableItem[] = [{ name: "type", remark: "选择类型", type: "Enum", optional: ["radio", "checkbox"] }];

export default list;

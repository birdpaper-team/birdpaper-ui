import { PropTableItem } from "../../../components/table-block/src/tableBlock";

const list: PropTableItem[] = [{ name: "size", remark: "间距大小", type: "enum | number", default: "normal" }];

export default list;

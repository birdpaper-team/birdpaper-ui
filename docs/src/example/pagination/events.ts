import { EventTableItem } from "../../../components/table-block/src/tableBlock";

const list: EventTableItem[] = [
  { name: "page-change", remark: "页码改变触发", params: "page" },
  { name: "size-change", remark: "每页条数改变触发", params: "size" },
];

export default list;

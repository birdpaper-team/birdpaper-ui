## 基础用法

鼠标悬停在元素上时显示提示信息。

<demo-block src="tooltip/basic">

<<< @/example/tooltip/basic.vue

</demo-block>

## 弹出位置

通过 `position` 属性设置弹出位置，支持 `top`（上方，默认）和 `bottom`（下方）。

<demo-block src="tooltip/position">

<<< @/example/tooltip/position.vue

</demo-block>

## 主题

通过 `theme` 属性切换主题，支持 `dark`（深色，默认）和 `light`（浅色）两种风格。

<demo-block src="tooltip/theme">

<<< @/example/tooltip/theme.vue

</demo-block>

## 触发方式

通过 `trigger` 属性设置触发方式，支持 `hover`（悬停，默认）和 `click`（点击）。

<demo-block src="tooltip/trigger">

<<< @/example/tooltip/trigger.vue

</demo-block>

## 自定义内容

使用 `content` 插槽自定义提示内容，可展示更丰富的结构化信息。

<demo-block src="tooltip/content">

<<< @/example/tooltip/content.vue

</demo-block>

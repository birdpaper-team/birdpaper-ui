## 基础用法

通过 `#content` 插槽放置 `bp-doption` 选项，`@select` 事件获取选中值。

<demo-block src="dropdown/basic">

<<< @/example/dropdown/basic.vue

</demo-block>

## 触发方式与位置

通过 `trigger` 属性切换触发方式（`click` / `hover`），`position` 设置弹出位置。

<demo-block src="dropdown/trigger">

<<< @/example/dropdown/trigger.vue

</demo-block>

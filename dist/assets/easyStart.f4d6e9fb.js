const t={},e=`<h1>Easy started</h1>
<p>This document will explain how to use Birdpaper-UI</p>
<h2>Import</h2>
<p>Not only can you import the whole package in its entirety, but you can also load components on demand based on actual usage requirements.</p>
<p>In <code>main.js</code> write:</p>
<pre><code class="language-javascript">import BirdpaperUI from &quot;birdpaper-ui&quot;
import &quot;birdpaper-ui/dist/index.css&quot;

const app = createApp(App)
app.use(BirdpaperUI).mount(&quot;#app&quot;)
</code></pre>
<h2>Load on demand</h2>
<p>Update the documentation for this section later...</p>
`;export{t as attributes,e as html};
